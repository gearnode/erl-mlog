[{mlog,
  [{formatter,
    #{debug => false,
      format => rfc5424}},
   {level, info},
   {device, syslog},
   {application_name, "eventline"},
   {syslog,
    #{host => <<"localhost">>,
      transport => tcp}}]}].
